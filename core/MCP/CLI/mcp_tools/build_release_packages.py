#n()
    maimain__":__ == "___nameif _se!")

 releay for GitHub Readn🚀"\\
    print(e()}").absolut_dirder.outputilable in: {bu availPackages print(f"📦 (s)")
   kage} pac(packages)uilt {lenlly bn✅ Successfuf"\\int(  pr
    s)
  gems(packachecksulder.create_  bui
      = [package] packages     
   
        ackage()pment_peate_develoer.cre = buildag    pack":
        "devackage ==  args.p elif)
       sb_package(e_ueater.cr= build   package 
          "usb":package ==f args.  eli    kage()
  _docker_paceateder.cr builckage =    pa      
  ocker":"dge ==  args.packa       elifge)
 gs.packapackage(arable_.create_portlder= buige ka    pac       nux"]:
  "li, "macos",ndows""wie in [ args.packag  if    )
  d_dir(lean_buil builder.c      
 )
    else:kages(ll_pac_alder.builduikages = b       pac
 "all":kage == pacif args.
    )
    t_dirutput_dir=args.ooutpun, sios.ver(version=argBuilderseMCPRelea  builder = 
    
  gs()r.parse_arparseargs = 
    )
     to build"kage type, help="Paclt="all"    defau                  
 , "all"], "dev" "usb", ",docker", ""linux"macos", windows", ["oices=age", chpack--_argument("dd   parser.a")
 tory direcOutput", help="="releaseslt", defauut-dirtp"--out(menadd_argu parser.   on")
ersilease velp="Re"1.0.0", hult=efaon", dersi--vargument("ser.add_    par)
kages"lease pacreBuild MCP n="r(descriptiosePargument argparse.Arparser =ain():
    def mpath}")

checksums_ile: {ecksums fed ch"Creat.info(fogger     l   
   n")
     me}\\age.na  {packksum}hec(f"{c    f.write              
  ()h.hexdigestha256_hasm = ssu     check                 
           k)
       (chunpdateh.u_has   sha256                 ):
        4096), b""a: pf.read(n iter(lambdk ir chun      fo           f:
       "rb") as package, (penop       with             sha256()
  hashlib.h =6_has       sha25      
       ):is_file(nd package.ts() a.exiskageac      if p
          packages:ckage in     for pa       as f:
  "w")ms_path, en(checksuwith op
        "
        txton}.{self.versiSHA256SUMS_v / f"ut_dirtp = self.oums_pathksu      chec    
hlib
      import has
        ges""" all packa forhecksums SHA256 c"Create    ""    th]):
es: List[Papackag, ums(self_checksdef create
    
    rn packages       retu      
 ")
  )}.absolute(f.output_dirin: {selailable ges avackar.info(f"P       logge")
 packageselease ages)} rn(packd {lelly createuccessfur.info(f"S      logge
          ckages)
hecksums(paelf.create_c  s   ksums
   te chec     # Crea    
     {e}")
   notes: seate releao creailed terror(f"F     logger.      e:
 eption as Exc     except   otes)
 d(release_ns.appenage       packes()
     se_noteate_relea = self.crnotesrelease_         :
     try   
   ase notes Create rele
        #       }")
 {eage: lopment packeate deveiled to cr.error(f"Fagger     lo       :
 eException asept  exc
       kage)pacd(dev_es.appen  packag      ge()
    opment_packacreate_devel = self.ckagev_pa    de      try:
    
       packagementdevelope    # Creat     
     e}")
   ckage: { paeate USBcrd to "Faile(ferrorgger.    lo:
        ion as eeptexcept Exc    age)
    nd(usb_packes.appeackag  p        age()
  _usb_pack.create selfpackage = usb_    :
        try      kage
 rtable pacte USB po Crea    #
        }")
    : {eker packageeate DocFailed to cr.error(f"    logger
        n as e:tiopt Excepexce      
  ge)er_packadockend(.apppackages           kage()
 docker_pacf.create_selackage = ker_p    doc       y:
      trkage
   ocker pacCreate D        #      
")
   ckage: {e}m_name} paate {platfor creiled toFaerror(f"ogger.   l        as e:
      Exception     except    
    ge_path)packad(ges.appencka    pa        name)
    e(platform_ckage_paortablf.create_ph = selage_pat     pack          try:
           ux"]:
  s", "lin "maco"windows",n [m_name i for platforrm
        platfoges for eachkae paceate portablCr  #          
= []
     ackages    p 
     r()
       build_di.clean_       self
 )
        ..."kages release pacg allBuildin("info logger."
       ges""e packall releas""Build a
        "s(self):all_packagebuild_   def 
    
 notes_pathe_rn releas      retuth}")
  tes_palease_nore notes: {ed releasenfo(f"Creatogger.i
        l      
  e_notes)releas   f.write(:
          "w") as ftes_path,noelease_ith open(r  w  md"
    rsion}..vev{selfSE_NOTES_ f"RELEAr /.output_dilfs_path = se_note  release          
    
'''
opment!** 🎉elwered dev of AI-pohe futurecome to tel🎉 **W!**

w todayrkflot wopmenur AI develo yoatingcelerrt acstand platform ar your e fopackagiate propr the apad

**Downlo---atures

rch feresea advanced tion andtegra inel**: LL0 modtsemenancuture Enhons
- **Fand suggestiience erre your expack**: Sha Feedbity **Communt
-ronmenvient enour developm in y Deploy MCP*: Deployment*Productions

- ** 📈 Next Stepools.

##evelopment tered dAI-powment in advanceificant signts a sentor repreceleraow Acorkflic WMCP Agentle. The lease possib this remakeed ers who helpers, and usutors, testall contrib to ouhank ygments

Ted 🎊 Acknowl
##get help
and s cussionJoin disunity**: 
- **Commreseatuequest fbugs and rt **: ReporIssues*GitHub - *ages
 packd in alludecldes ine guiensiv: Comprehion**ocumentat- **D

rtuppo

## 🤝 Sen resolvede bes havy issueh-prioritand higll critical e
- Ahis releasor teported fone rsues

- NIs## 🐛 Known 

e type.hosen packagor your cctions ftruinsion allatw the instation
FolloInstall-Time 
### Firstck`
-che `mcp healthity:unctionalify fon>`
4. Ver <old_versirom-version migrate --f: `mcpityration utilig m. Run theersion
3e new vll thad and instaDownlota
2.  daion andratonfigung cour existiup y. BackVersions
1revious 
### From Pions
uctrade Instrpg
## 🔄 Us
ent readineson deploymroductiring
- P monitoation andizptimormance o- Perf
tion suitelidang and vaive testiehenson
- Comprtiput validath inramework wicurity fComplete se- lity
 Quay &curit🔒 Sery

### ecovend r handling aornsive erromprehe- Cck
lbaith rol system wpdatetomatic uport
- Au suprationintegrsal IDE Univees
- ckagrtable pa with ponstallationified iSimplnce
- xperie Eed User 🚀 Enhanc##ization

#timtion and oplida consoed memorync- Advachmarking
 global benge withanchic data exeneton
- P2P gl adaptationmentaenvirith er system wnetic trigglation
- Geodurmone mith hoing wsharata sensory d Cross-lobe es
-urgnitive Featdvanced Co

### 🧠 Aguidesd mentation ansive documprehen- Copackages
ment dy deployn-rearoductiosted
- Pd and teementeimplnts nepoomm cajor syste%)
- All m (98% → 100mentationplete Imple
### ✅ Com
version}n v{self.at's New i## 🎯 Whls

detaitation  implemenm design and*: Systeuide*hitecture Ges
- **Arcidelintion gutribut and conDevelopmenGuide**: loper  **Devereference
-e API et Complon**:entatiocumPI D
- **AelytivMCP effeco use ow tde**: H **User Guitructions
-etup insep sep-by-stGuide**: Stnstallation  **Is:
-ll packageluded in ation is incmentamplete docu

Coionumentat
## 📚 Doc8.04+)
 (Ubuntu 1 Linux0.15+, orcOS 1ws 10+, maOS**: Windonded
- **recommeGB mum, 20e space mini fre2GB**: - **Storaged
deGB recommenum, 16B RAM minimemory**: 4Gs)
- **Mckage pan portabled iudeer (inclr high.8 o*: 3ython**Pnts

- *Requiremeystem 
## 🔧 S0
300ost:ocalh/l http:/cess atows)
3. Acindusb.bat` (Wcp_ or `start_minux/macOS).sh` (Lart_mcp_usbn `st
2. Rudrive to USB n}.zip`lf.versiotable-v{semcp-usb-porct `
1. ExtraSB Portable# U
##```
se up -d
poocker-com
dnd runuild ar

# Bcd mcp-docke}.tar.gz
elf.versionr-v{s-docke mcpar -xzf
tker packagect Doc Extra`bash
#yment
``loDepker # Doc
##mcp.bat`)
r `start_mcp.sh` o`./start_P (
4. Start MCbat`)ll. `install.sh` orler (`instan the instaln
3. Rued locatiour desiro yotract torm
2. Exatffor your plackage riate ppropd the apnloa
1. Dowkage)e PacablPort Start (Quick
### tion
llaInsta

## 📋 x supportand Linu macOS, Windows,**: -Platform- **Crosspabilities
ollback catem with rng syself-updati**: Satic Updatesom
- **Autiesencendxternal depwith no eenvironment ntained lf-co**: SeDeployment*Portable ns
- *iontegrat ind customde, aauCl Cursor, e,t**: VS CodpporSuIDE al *Univers
- *& Deploymentegration g

### Intnchmarkine and be exchangtaenetic dad gntralize Dece*:tion*llabora*P2P Coent
- *nagemmarce  and resouimizationtime optReal-: **onitoring Mrmance **Perfo LLMs
-ation forext genercontnt -efficie*: Tokenion*t Optimizat**Contex- racking
ndings tfiflows with workrch d reseauideomation**: Guth A**Researc
- nscatiopliaplete o compts intompform prent**: Transt Managemjecligent Proelnties
- **I Capabilit Advanced
###
ge data exchanth P2Pn witatioal adap Environment*:ution*volnetic EGety
- **e sensitiviaptivh adon witlaticolumn simu Neural **:ecognition**Pattern R-  lobes
vegnitien coetwering bata shasory dd senne-triggereon**: Hormo CommunicatiLobe**Cross-ation
- idoltic consmawith automemory  long-term term, andt-hor: Working, sory System**e-Tier Memhre **Tre
-rchitectued Airn-Insp

### Braiy Features
## 🚀 Kent
t environme developmen.gz` - Full.tarlf.version}ment-v{sedevelop: `mcp-nt**lopmeDeveve
- ** driun from USB R` -ersion}.zipv{self.vportable- `mcp-usb-*:able* Portent
- **USBploymerized de - Containion}.tar.gz`-v{self.versmcp-dockercker**: `ages
- **Dolized Packia
### Spec.gz`
rsion}.tar.veself-linux-v{p-portable`mcLinux**: `
- **r.gzersion}.taelf.vmacos-v{sortable-cp-pcOS**: `m **maip`
-.zon}siverv{self.-windows-p-portable `mc*:s* **Windowmmended)
-ckages (Recoe Partabl## Poages

#Packble Availa

## 📦 ities.t capabildevelopmen AI d advancedecture and architrespibrain-insive  comprehenithlerator w Acce Workflow Agentic the MCP ofcompletione  marks thThis release
tion
plementa Imomplete Release - C🎉 Production## version}

{self.rator v Accelelowtic Workf'# MCP Agen_notes = f''   release""
     e"e notes file releaseat""Cr   "   -> Path:
  tes(self) e_release_no def creat   
   me)
 rce_dir.na arcname=sour,(source_di    tar.add:
        tar 'w:gz') as e_path,.open(archivtarfilewith   "
      chive""Z arate TAR.GCre" ""       th: Path):
rchive_paPath, air: _df, sourceelve(shie_tar_arc  def creat   
  arcname)
 h, patile_pf.write(f          zi    )
      _dirrcee_to(souelativpath.r= file_rcname  a                  _file():
 ath.isle_p      if fi         lob('*'):
 ir.rg_din sourcee_path    for fil:
         as zipfP_DEFLATED) ipfile.ZI 'w', zpath,ive_archile(zipfile.ZipFith "
        w"ive"e ZIP archeat   """Cr:
      Path)hive_path: Path, arcce_dir:ourf, sarchive(selreate_zip_   def c 
 ath
   ive_pch return ar")
       hive_path}age: {arct packmend developCreateger.info(f"      log 
       )
  rchive_path_dir, achive(deve_tar_ar self.creat     "
  ar.gz.tion}f.vers-v{sellopmentf"mcp-deve / dirt_puoutlf.path = sehive_arc     
   ivech # Create ar
        n)
       setup_widev_write(       f.f:
     ) as ", "w".bat "setup_devv_dir /ith open(de
        w  '''
      o.
pause

echrc/mypy s: ckingn type checho To ruake8 src/
enting: flun liecho To rst
 tests: pyte To runhoate.bat
ecctivScripts\\a .venv\\t:ronmene envi activate thecho To
o.ted!
echmplecoetup  sntnt environmeDevelopmeho.
echo / -v

ect tests
pytes tests...g initialho Runninl tests
ecian init

REM Rut install-commi...
pret hooksmmire-coing pnstallks
echo Iommit hoore-cstall p .

REM Inl -estal.txt
pip inirements-dev-r requ install ip
ppipe radstall --upg
pip inncies...endeepelopment d dev Installingncies
echont dependemeelopInstall devbat

REM activate.ripts\\\Scenv\.venv
call  venv .vython -mnment...
p enviro virtual Creatingonment
echonvir eate virtualEM Cre
)

Rb 1
    exit /use
    paorghon.//pytttps:om h.8+ fr 3tall Pythonase insho Ple   ecTH
  PAnot inlled or  instathon is notror: Py   echo Ereq 0 (
 orlevel% nrrif %e1
ul 2>&on >n-versithon -e
pys availabl ihon if PyteckEM Cht...

Rvironmenpment enP develo up MCecho Setting@echo off
in = '''etup_w dev_s      up
 nt setdevelopmedows eate Win     # Cr      
   55)
  sh", 0o7etup_dev. / "sev_dirhmod(d   os.cp)
     v_seturite(de      f.w
      ") as f:.sh", "wsetup_dev" / en(dev_dir    with op    
    
     ""
'''cho"
emypy src/king: type checun  "To r
echo"lake8 src/n linting: fTo ruecho "st"
sts: pyte "To run tete"
echoivan/actvenv/birce .ent: souhe environmvate t"To acti"
echo "
echo "! completedment setupment environ"Develop"
echo cho "ts/ -v

ees"
pytest t..ts.itial tesning inho "Runs
ecl testian init
# Ru
stallmit in."
pre-comks..e-commit hooalling prho "Inst hooks
ecl pre-commit
# Instal .
p install -ev.txt
piirements-de-r requstall  inde pip
pippgra--upip install .."
encies.endt dep developmen"Installinges
echo ncindeent depeopmstall develate

# Inin/activvenv/bce .ur
so.venv -m venv on3
pythnment..."tual enviroireating vho "Crt
ecironmenrtual env# Create vi


fi1   exit ersion)"
 n_v$pythod (found  is requireerr high_version o $requiredror: Python echo "Er  n
 n" ]; theed_versiorequir1)" != "$head -n| sort -V | on" thon_versin" "$pyred_versio "$requi\\n'f '%s"$(print
if [ 
on="3.8"red_versirequi[:2])))')
n_infosys.versiotr, join(map(sint(".".ys; prport son3 -c 'im(pythn_version=$ion
pytho vershonck Pyt
# Che"
...entonmirment envopevelCP dp Metting u
echo "Sset -e
sh
ba= '''#!/bin/ dev_setup     script
   pment setup te develo # Crea           
tem)
    dir / i, dev_.copy2(src      shutil           lse:
             e'))
      pyc', '.git '*.pycache__',s('__atternil.ignore_pe=shutitem, ignorv_dir / src, decopytree(  shutil.               ir():
   .is_dif src              s():
  src.existf          i   item
  /oject_root self.pr     src =    es:
   dev_filtem in        for i
        
        ]
 fig.yaml"e-commit-con    ".pr      i",
  y.in "myp          ini",
 "pytest.            tignore",
".gi         ION",
      "VERS   
      NSE","LICE            DME.md",
   "REA         
up.py",set       "    ml",
 ct.to  "pyproje
          dev.txt",ements-    "requir        ts.txt",
menquire    "re      ",
  examples/    "      nfig/",
          "co
    ripts/","sc   ,
         "docs/    "
        s/", "test    ,
           "src/"
        les = [ev_fi
        dtoolslopment vecluding des inilerce fall sou# Copy        
    
     e)=Trust_okr.mkdir(exidi        dev_elopment"
"mcp-devld_dir / f.bui = selir  dev_d
              age")
nt packlopmeating deve.info("Cregger
        lo"tools""ource and h full sckage witment pae developreat  """C    
  f) -> Path:e(selnt_packagevelopmee_d def creat
       chive_path
 ar     return  h}")
 ve_pathirce: {ackagrtable paSB pod Uate.info(f"Cregger   lo   
     path)
     hive_arcb_dir, e(usivrcheate_zip_af.cr   selzip"
     }.sionlf.verble-v{seb-portap-usir / f"mcut_d= self.outpe_path   archiv     ive
  arch   # Create      
      
 eadme)b_re(usrit f.w       s f:
    , "w") aE_USB.md"r / "READM open(usb_di  with              
der.
'''
olocs/` f the `dentation inocumhe full d, see tor more helpS)

FcOnux/maLith sudo (dows) or wior (Winstratmininning as ad3. Try ruSB drive
e Uo thissions t permwritehave k that you led
2. Checnstal+ is in 3.8Pythonsure  Esues:
1.r isnteouu enc

If yootinghoesTroubl
## h Python
tem witny sysks on arun
- Worup on first settic 
- Automanvironmentrtual evid neaif-cont
- Selon required installatie - notely portabl- Comple

tures

## FeariveUSB don  free space  2GBastp
- At le setur initialnection fot conInterne- em
st syst hoon thenstalled  higher i orPython 3.8

- ntsRequireme000

## host:3//localMCP at http:ccess 
4. A only) runstmplete (fir to coetup s Wait forb.sh`
3.rt_mcp_usta/sn `.Ru
2.  directoryo thise tatd navigrminal an1. Open teux
 macOS/Lin
###
calhost:3000at http://loss MCP cce3. Aly)
run onst plete (fir to comsetup for . Wait
2usb.bat`tart_mcp_lick `sDouble-c
1. # Windows

##Quick Startive.

##  USB drctly from arun diren tor that caAcceleraWorkflow entic P Ag MCof theersion le va portabThis is 

ackageB Portable P'# MCP USme = ''sb_read  u      package
 USB ADME for RE # Create 
            win)
  cher_e(usb_laun  f.writ       :
   w") as f.bat", "_usbart_mcp / "stpen(usb_dirwith o         
       e
'''
us3000

part -po0.0.0.0 -t r --hosi serve src.mcp.cln -m
pythover...ting MCP sero Starer
ech servart MCPM St)

REe.bat
at\activcripts\v\\S  call .ven
  se () elleted!
 Setup comp echo    
 all -e .
   instt
    pipirements.txll -r requnsta
    pip ie pipupgradall --    pip instdencies
tall depen REM Ins
    
   ate.batctiv\\a\\Scripts call .venvenv
   env .v v  python -mnt
  ironmertual enveate vi Cr REM
    
   1
    )  exit /b se
      au        puse MCP
 3.8+ to onPythall e insteaso Pl echtem
       is sysled on thstalon is not inPytho Error:   ech0 (
       neq vel%lef %error
    i>nul 2>&1ersion n --vho
    pytlabilityavaiheck Python EM C R
    
   ..use.P for first tting up MC Se  echo
  env" (t ".v
if not exisexistsnvironment  virtual eM Check if%

RE%CDo Location: chdrive...
eUSB  MCP from ho Startingdp0"

ec
cd /d "%~ndows
Wiuncher for le LatabPor USB M MCP off
RE '''@echon =_launcher_wiusb
        cherundows USB la Win Create 
        #       o755)
.sh", 0cp_usbart_m / "st(usb_dir.chmodos   cher)
     unwrite(usb_la   f.:
          "w") as f",shb._mcp_usstart"r / usb_di open(     with     
   
'''
   ..."r to exit"Press Ente-p 
read port 3000
--0 0.0.0.--host cli server  -m src.mcp.thon."
pyMCP server.."Starting echo r
t MCP serve
# Star
fi
ivatectnv/bin/aurce .ve
    so"
elseeted!ompl c"Setup   echo   
 all -e .
  inst
    pip .txtuirementsr req install -  pipade pip
  l --upgr  pip instalencies
  pendnstall de    # I  
ctivate
   .venv/bin/a
    sourcem venv .venvpython3 -    t
onmennvirual ereate virt  # C
    
   fit 1
      exi    xit..."
 to eess Enter p "Pr   read -MCP"
     se o uhon 3.8+ tall Pyt inst"Please      echo ystem"
  is sthstalled on is not inhon 3 "Error: Pytecho         l; then
 /dev/nul &>python3command -v    if ! bility
 vaila Python a    # Check

    e..."or first usp MCP fing u"Sett
    echo v" ]; then ".ven ! -dexists
if [t ironmenenvvirtual k if hecR"

# C_DISBtion: $ULoca"
echo "B drive...rom USing MCP f"Start"

echo "$USB_DIR
cd "d)&& pw0]}")" BASH_SOURCE[rname "${ "$(diR="$(cd_DIated
USBpt is locriis sc where thctoryt the direer

# Gech Laun PortableUSBsh
# MCP ''#!/bin/bauncher = 'la      usb_uncher
  ic laB-specifreate US      # C    
  b_dir)
    (usfilesce_y_sour  self.cop   
   source file    # Copy s      
   )
   ok=Truexist_dir.mkdir(e        usb_e"
rtabl"mcp-usb-pold_dir / elf.bui usb_dir = s  
       ")
      e package portablg USB("Creatinlogger.info"
        ""ackagetable pB poreate US""Cr
        "-> Path:ge(self) b_packaus def create_h
    
   hive_pateturn arc
        r")ath}hive_pge: {arcr packaeated Dockeo(f"Cr  logger.inf    
      path)
     archive_cker_dir,e(dorchiv_areate_tar    self.c"
    r.gzn}.taersiof.ver-v{selmcp-dockt_dir / f"lf.outpuve_path = sechi arve
       rchi# Create a       
  ir)
       r_diles(dockece_fsourcopy_       self.iles
 y source fop  # C
             5)
 h", 0o75ild.s"buer_dir / (dock.chmod       os
 uild_script)f.write(b          
  ") as f:", "w"build.shr_dir / (dockewith open            
   
'''
  up -d"compose"docker-"
echo se:docker-compor use "O""
echo "
echo estow:latkfl-woragenticerver mcpai/ mcp-sname000 -- 3000:3er run -d -p"dockho "
ecainer:the conto "To run 
ech
echo ""ully!"uccessfbuilt simage er o "Dock

ech'' ..version + 'selfw:v''' + florktic-woagenai/ -t mcpker build .
docw:latestic-workflontcpai/aget mld -ker buie
doce imagild th"

# Bu...ker imageocing MCP DBuild
echo "
ash
set -e/b/binript = '''#!  build_sc
      pt scriker build Doc  # Create    
      
    content)write(nginx_     f.    
   "w") as f:", nf/ "nginx.cor _dierpen(dock oth    wi      
      ''
  }
}
'      }
  
   $scheme;warded-Protoder X-Foroxy_set_hea     pr      ;
 rded_for_add_x_forwaFor $proxyd-X-Forwardeader oxy_set_he         pr
   r;emote_addReal-IP $rader X-t_he  proxy_se       t;
   Host $hoset_header proxy_s    
        end;://mcp_backs httpasproxy_p           {
 tion /         loca

t;me localhosnar_rve   se   n 80;
  te      lis {
      server

    }00;
30ver:-sermcp     server end {
   eam mcp_backpstr{
    utp 4;
}

htns 102ectior_conn{
    workeents nt = '''evnx_conte     ngi
   urationnfigate nginx co   # Cre   
     t)
     _conten(composef.write           s f:
 "w") aml", r-compose.y/ "docke(docker_dir penith o
        w  
      ''' mcp_logs:

 nfig:mcp_co
  cp_data:lumes:
  mped

voops-start: unlesster
    rerv  - mcp-ses_on:
    
    dependginx/ssl:ro./ssl:/etc/n- f:ro
      .coninx/nginxconf:/etc/ng./nginx.
      - mes:    volu3"
"443:44"
      - 80  - "80:    :
    portspine
ge: nginx:al:
    ima  nginxoxy
verse pr Add a re # Optional:s

 40art_period:  st    etries: 3
 
      rut: 10s   timeo  
 0sal: 3  interv]
    0/health')"t:300calhosp://lot('httquests.geret requests; mpor "-c", "ihon",CMD", "pyt   test: ["eck:
    healthchstopped
   t: unless-star  rets
  rojec=/app/data/pT_PATHEC- MCP_PROJ     
 ODE=falseUG_M - MCP_DEB   
  INFOEL=OG_LEV- MCP_L     onment:
 
    envirpp/logsogs:/a ./lg
      -/app/confifig:./conta
      - daata:/app//d- .
      es:    volum0"
00"3000:3      - ports:

    uild: . b   
erver:cp-s  ms:
servicen: '3.8'

'versiontent = ''pose_co        commpose.yml
coate docker-# Cre           
   
  ontent)le_cockerfite(dwri f.         ") as f:
  wile", ""Dockerfr_dir / n(dockewith ope            
 
   "]
'''3000t", ""--por0.0.0", ", "0., "--host" "server",cp.cli.m, "srcon", "-m""pyth [d
CMDcommanfault # Det 1

')" || exiealthhost:3000/http://locals.get('h requestrt requests;-c "impoD python \
    CM=3 \-retriesriod=5s -tart-pes --s=100s --timeoutrval=3ECK --inteEALTHCHck
H Health cheE 3000

#port
EXPOSse 

# Expop/logs /appp/configdata /a -p /app/
RUN mkdirtoriesireconfig dta and ce da
# Creatp
 mcERapp
USmcp:mcp /n -R chow    \\
  mcp && -m -u 1000radduseUN oot user
R non-rCreate
# e .
 install -ipUN pt mode
Rlopmen MCP in deve
# InstallCOPY . .
e
n cody applicatio
# Cops.txt
irementequhe-dir -r ractall --no-c ins pip\\
   de pip && upgrair --ache-dtall --no-cip inss
RUN pdencieepenPython dll nsta I/

#xt ..tdevs-rement.txt requients requirem
COPYachingtter cor bets first fy requiremen

# Copists/*ib/apt/l-rf /var/l&& rm  \\
    ialuild-essent\
    b\   curl  \\
 \\
    gitinstall -y pt-get && aget update pt-UN acies
Rependentem d sysll

# InstappWORKDIR /aectory
dirt working lim

# Se3.11-spython:M 'FROnt = ''erfile_conte      dock
  e DockerfileCreat  #   
      )
      ueTrir(exist_ok=r.mkdker_di        dococker"
p-d"mc/ ir .build_d= selfocker_dir  d      
  ")
       er package Docko("Creating  logger.inf      """
lescompose fiile and ckerfwith Dokage  Docker pac"Create"    "ath:
    > Plf) -kage(sedocker_pace_f creat   
    de
 55)", 0o7-portable / "mcpe_dirckagpa   os.chmod(        _content)
 uncherrite(laf.w     
           s f: "w") ae",portabl"mcp-ir / ackage_den(pith op         w   
'''
t 1
fi"
    exirst.tall.sh fise run insnd. Pleant not founvironme"Virtual e
    echo @"
elser "$p.cli serveon -m src.mcythvate
    penv/bin/acti  source .v
  te ]; thenactivav/bin/[ -f .ven0")"
if (dirname "$d "$/bash
c = '''#!/bintentlauncher_con        
      else:      nt)
ontecher_ce(laun    f.writ     
       ") as f:at", "wrtable.bpomcp-r / "package_di open(     with
       ''e
)
'.
    pausbat firstall.n instase ruund. Plent not fonme envirotual  echo Vir (
  else%*
) server rc.mcp.cli ython -m sbat
    p\\activate.sipt\Scr\all .venv cat (
   vate.btis\\ac\\Scriptenv exist .v
ifd "%~dp0"o off
cd /echtent = '''@_coner    launch       
 ows":= "windrm_name = platfo      if""
  "her scriptsauncle lte portabCrea  """      e: str):
form_namath, platage_dir: Pr(self, packnchee_laublrtaate_po
    def creo755)
    all.sh", 0st / "inkage_dir(pacs.chmod   o    table
  Make execu       #  
 t)
      r_contene(installe  f.writ       as f:
    h", "w")stall.sin "ge_dir /kath open(pac    wi      
    e"
'''
  anywherfrom to use MCP ~/.bashrc' rce soul or run 'ermina turstart yo "Re
echo
echo "" directly"pt scri mcp-server"Or use theh"
echo _mcp.s: ./startr, run serveCPo start M"T"
echo "
echo ly!"essfulted succon complelatiInstal"
echo "
echo "hrc"
fi
in ~/.bas to PATH "Added MCP
    echo bashrcd)" >> ~/.$PATH:$(pw\export PATH=ho "en
    ec:"* ]]; th":$(pwd)ATH:" != *":$Pal)
if [[ ionTH (opto PAdd t
fi

# Ay"sktop entrde"Created 
    echo ment;
EOFevelopgories=Dtecation
Cape=Appli=true
Tynal
Termion.pngts/mcp-icassepwd)/docs/ver
Icon=$(p-serec=$(pwd)/mcr
Exw Acceleratoc WorkfloAgentiComment=MCP er
ServP 
Name=MCy]Desktop Entr< EOF
[r.desktop <ve/mcp-serications/share/appllocalcat > ~/.ons
    icatiare/appl/shcalir -p ~/.lo mkd  ]; then
 T_DESKTOP" $XDG_CURREN[ -n "ble)
if laavaiment is top environskif dep entry (deskto# Create erver

x mcp-shmod +
EOF

c"$@"r cli serve-m src.mcp.e
python ctivatbin/av/source .ven"$0")"
rname $(di/bash
cd "
#!/bin 'EOF'r <<vesert > mcp-
calauncher-wide eate system.

# Cr install -e e
pipent modin developmtall MCP 

# Insts.txtquiremenrell -r pip instaade pip
all --upgrpip insts..."
cie dependenInstallingo "encies
echl depend
# Instalctivate
n/arce .venv/biment
souvironrtual enivate vi
# Act.venv
3 -m venv ython..."
pntl environmeing virtuareato "Cronment
echal envie virtu
# Creatxit 1
fi
sac
    e ;;
    e    r"
       nageackage maution's pour distrib+ using yython 3.8 Pllstainse  echo "Plea           *)
      
    ;;         "
 thon-pipn py-S pythoo pacman  sud with:"Install   echo         )
 arch       ;
           ;3-pip"
  honthon3 pyt install py sudo dnfstall with: echo "In           os|rhel)
 fedora|cent            ;;
 v"
      hon3-venhon3-pip python3 pytyt pinstallt apate && sudo do apt updith: sull w "Insta      echo  
    ebian)u|dbunt        uTRO in
DIS  case $talled"
  3 is not insython   echo "P
  ull; thenn3 &> /dev/nthoommand -v py ! ce
ifis availabl Python eck if# Chwn"
fi

unknoDISTRO="
else
    TRO=$IDe
    DISreleasetc/os-    . /
ene ]; threleas /etc/os-[ -fif on
t distributietec

# D."elerator..flow AccrkP Agentic Wolling MCo "Instaech

-esh
set ''#!/bin/bat = 'ler_contental        insr"""
tallefic insinux-speci"Create L    ""):
    Pathackage_dir: lf, pller(seux_instate_lindef crea      
  ", 0o755)
nstall.sh"ickage_dir /  os.chmod(pale
        executabke       # Ma
        
 content)ller_ite(insta        f.wr
    ) as f:"w"", install.shge_dir / "kaopen(pac   with             
 d"
'''
p' commanhe 'mc tto use ~/.zshrc' run 'sourceminal or  your tero "Restarto ""
echy"
echdirectlver script  mcp-ser the"Or use
echo .sh"start_mcp ./erver, run:art MCP scho "To st""
echo 
eully!"sfsucceseted mplllation costa"
echo "Ino "le

echprofibash_'" >> ~/.ervermcp-s'$(pwd)/ias mcp=o "alll || echev/nu.zshrc 2>/d" >> ~/rver'cp-sep='$(pwd)/malias mc"
echo "...g aliasho "Creatin
ecoptional) alias (aterer

# Crvemcp-sechmod +x 
F
"$@"
EO server  src.mcp.clie
python -mn/activatv/bie .ven"
sourc"$0")(dirname "$
cd n/bash#!/biF'
rver << 'EOcat > mcp-se
unch scriptte la
# Creal -e .
ip instalnt mode
pin developmeP Install MC
# nts.txt
emer requirl -p instalpipip
-upgrade install -ip ies..."
p dependencling"Instalho ncies
ecll depende

# Instaatebin/activnv/rce .veounment
siroal envvirtuctivate 

# Avenv -m venv .
python3ment..."tual environeating vir
echo "Crnmentrtual envirovi

# Create 
fixit 1.11"
    ethon@3install pyho "brew 
    ecebrew:" or use Homn.orgho//pytm https:ro.8+ f 3ll Pythonase instaho "Ple   ecalled"
  is not instthon 3 "Py echo then
   dev/null;hon3 &> /-v pytnd mmaif ! colable
thon is avaif Py
# Check iator..."
erflow Accelc Worknti Ageg MCPnstallin"Iecho 
set -e

#!/bin/bash= '''nt ler_contealnst
        i"""stallerc inecifite macOS-speaCr """      ):
 e_dir: Pathlf, packager(ses_installate_maco   def cre5)
    
 t", 0o75"install.bage_dir / ckad(pamo  os.chle
      ke executab       # Ma    
 ent)
    contnstaller_ f.write(i        s f:
   ", "w") all.bat / "instaage_dirck open(pa     with      
   '''
  se
echo.
paucut
 shortesktopck the de-cliublecho Or domcp.bat
 start_server, run:rt MCP To stacho.
echo !
eessfullyucc sn completedtio Installa
echoho.

ec.Save()}"ut'; $Shortc = '%CD%gDirectoryorkinhortcut.Wcp.bat'; $S\start_mD%\= '%Cath argetPt.Tortcu $Sh);Server.lnk'MCP \\ktopDes\\ERPROFILE%US('%hortcutreateShShell.Ccut = $Ws$Short; ellt.Shect WScripomObjt -cObjec = New-ll"& {$WshShe -Command 
powershellhortcuts...Creating sl)
echo (optionap shortcut ate desktoCre

REM install -e .
pip ent modeopmin develll MCP M Instab 1
)

REit /
    ex   pausedencies
 l depend to instal echo Faile0 (
   el% neq errorlevs.txt
if %requirement -r pip installpgrade pip
stall --uip inies...
pdencling depenalInstcho ndencies
epetall de Insat

REMactivate.b\\riptsSc\env\ .vllonment
cartual envirte vi
REM Activa)
 /b 1
   exit    pause
 onment
tual envireate virto crho Failed 
    ecl% neq 0 (rleveroif %ernv
env .ve-m von 
pythment...ironrtual envting vi Creanment
echoroual envirt Create vi)

REMt /b 1
use
    exi  paon.org
  ://pythm https+ fro.8on 3install PythPlease cho    eH
  in PAT noted orinstall not ython is    echo Pneq 0 (
orlevel% &1
if %err2>n >nul on --versiolable
pyththon is avaik if Py
REM Chec...
eratorlow AccelrkfAgentic WoCP Installing Moff
echo ''@echo  = 'ntentnstaller_co  i      """
installercific indows-spe"Create W ""h):
       atir: Ppackage_dlf, taller(sendows_inswireate_ 
    def cth
   e_pachiv  return ar      }")
athe_pge: {archivble packaortaCreated pnfo(f"er.i       logg     
 path)
   ir, archive_ve(package_darchieate_tar_cr  self.         ar.gz"
 }.tsion-v{self.verform_name}le-{platrtabp-podir / f"mcelf.output_= shive_path    arc       else:
         _path)
 vehiir, arce(package_div_zip_archelf.create         s   "
.zipsion}self.ver_name}-v{platform-{ablecp-portt_dir / f"moutpuh = self.chive_patar       ws":
     "windo_name == atformf pl    it up
    Package i #      
       me)
   form_nae_dir, plather(packag_launcable.create_port    selfuncher
    le lareate portab# C           
r)
     ckage_dialler(painux_instcreate_lself.       
     :  # linux    elsee_dir)
    ckagpar(lestalacos_in.create_melf       s   acos":
  "morm_name == atf pl  elifdir)
      package_r(ws_installeindoeate_wf.cr sel          :
 "windows"== m_name  if platfor    ler
   stalific inpece platform-s     # Creat       
   age_dir)
 ackce_files(pf.copy_sour  sels
      ce file# Copy sour
        )
        k=Truedir(exist_odir.mkackage_       p"
 tform_name}able-{pla f"mcp-portir /.build_d_dir = self    package           
 _name}")
latformge for {ple packaating portab"Cre(fr.info     loggem"""
    platforspecificor a kage frtable paceate a poCr"""    h:
    at Ptr) ->m_name: self, platforpackage(se_eate_portabl   def cr    
 r}")
 {target_dice files topied sour(f"Cogger.info
        lo       tem)
 rget_dir / ita.copy2(src, il    shut          
            else:        
  ')).pyc', '.githe__', '*rns('__pycacattee_putil.ignorm, ignore=shdir / ite target_src,.copytree( shutil                   _dir():
  if src.is         s():
     c.existsr if     m
       teot / i_roself.projectc =   sr          s:
ileential_fin essm r itefo       
        
      ]
   li.py"_mcp_c "simple          ",
 li.py_c"mcp    
        _mcp.ps1",art"st          
  p.bat",  "start_mc         ",
 p.shart_mc   "st
         ", "VERSION       ",
    SELICEN   "         ",
.md"README          ",
  py"setup.            ",
roject.toml"pyp         ,
   -dev.txt"quirements  "re
          ",xtements.t    "requir      
  s/",cript "s         ",
  ocs/    "d
        "config/",         /",
    "src      [
     s = ial_filent        esse""
"t directoryes to targesource filtial senCopy es      """):
  dir: Path, target_les(self_fiurcepy_sodef co
        ry")
rectod did buil"Cleane(gger.infolo  
      ok=True)t_r(exisld_dir.mkdi  self.bui    ild_dir)
  bulf.rmtree(sehutil.     s
       .exists():ild_dirself.bu   if 
     ""rectory"ld dilean the bui    """C  
  _dir(self):ldbuian_   def cle
    
 lute()}")ir.absof.output_d {selrectory:"Output di(fogger.info     l  ion}")
 rsackages v{veease prelP "Building MCr.info(f logge   
          rue)
  k=Tdir(exist_obuild_dir.mk     self.   =True)
ir(exist_okt_dir.mkdself.outpu      
  ectorieseate dirCr     #    
        
")dPath("buildir = uild_f.b sel       parent
.parent.ile__)__froot = Path(f.project_el        s
(output_dir)t_dir = Path.outpuself       rsion
 sion = ve   self.ver
     s"):"release= r _dir: stoutput", r = "1.0.0ion: stlf, vers_(sedef __init_    
    
"""ckagesrelease paCP  various M"""Build
    der:leaseBuillass MCPRename__)

cogger(__getLgging.ger = lo')
logge)sssa%(mevelname)s - e)s - %(le(asctimO, format='%g.INFel=logginicConfig(leving.basg
logggginup loet Sing

#ogg
import lrgparse
import aal, Option Listport Dict, imtyping
from mport Pathm pathlib irm
frotfort pla
import jsonle
impo zipfiimportle
arfi
import tubprocessmport sl
iimport shutirt sys
impo
ost mpor
i"
kages
""elopment pac
- Devgestable packa por
- USBAppImage)M, es (DEB, RPtem packag
- Sysrationsand configuer images - DockLinux)
acOS, ows, ms (Windlone package standa
- Portableses:eaHub rel for Gitkagespaceployment  various dipt createsscr

This ge BuilderPackaelease CP R""
M
"ython3nv pin/e!/usr/b